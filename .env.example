PORT=:8080
LOG_LEVEL=debug
JWT_SIGN_KEY=user
JWT_EXPIRE_TIME=120
# 默认管理员账号（明文，程序启动时会自动 bcrypt 加密后存入数据库）
USERNAME=user
PASSWORD=password

# MySQL 配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-password
DB_NAME=scaffold_demo
DB_MAX_IDLE=10
DB_MAX_OPEN=100
